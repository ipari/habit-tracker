# Habit Tracker 제품 요구사항 문서(PRD)

## 문서 정보

- 문서 상태: 1차 출시 범위 확정
- 버전: 0.3
- 작성일: 2026-07-31
- 최종 수정일: 2026-08-01
- 대상 플랫폼: iPhone, Android, 데스크톱 웹 브라우저
- 서비스 형태: 개인용 자체 호스팅 PWA

## 1. 제품 개요

Habit Tracker는 사용자가 반복하고 싶은 습관을 등록하고, 날짜별 달성 여부와 연속 달성 일수를 확인하며, 습관 수행 요일의 정해진 시간에 알림을 받을 수 있는 개인용 웹 애플리케이션이다.

사용자는 현재의 연속 달성 성과를 습관별 배경화면이 적용된 세로형 이미지로 만들어 Instagram 등에 공유할 수 있다.

한 사람이 여러 스마트폰과 브라우저에서 같은 계정으로 접속해 동일한 데이터를 사용한다. 모바일에서 빠르게 오늘의 습관을 체크하는 경험을 최우선으로 하며, 앱스토어 배포 없이 홈 화면에 설치할 수 있는 PWA로 제공한다.

## 2. 문제 정의

사용자는 반복할 습관을 잊지 않고 실천하며 진행 상황을 간단히 확인하고 싶다. 메모나 일반 달력만으로는 다음 요구를 한 번에 해결하기 어렵다.

- 오늘 해야 할 습관을 빠르게 파악하기
- 실천 여부를 최소한의 조작으로 기록하기
- 놓친 과거 기록을 달력에서 보정하기
- 예정일과 추가 달성을 기준으로 연속 달성 상태 확인하기
- 습관 수행 요일의 원하는 시간에 스마트폰 알림 받기
- 여러 기기에서 동일한 기록 사용하기

## 3. 목표와 성공 기준

### 제품 목표

- 사용자가 오늘 수행할 습관을 앱을 연 직후 확인할 수 있다.
- 습관 하나의 달성 여부를 한 번의 주요 동작으로 변경할 수 있다.
- 날짜별 기록과 streak가 사용자가 예상하는 수행 요일 기준으로 일관되게 계산된다.
- iPhone과 Android에서 홈 화면 설치 및 푸시 알림을 사용할 수 있다.
- 개인 서버에서 이해하고 유지할 수 있는 작은 Python 중심 시스템으로 운영한다.
- 현재 성과를 보기 좋은 이미지로 만들어 모바일 공유 시트를 통해 손쉽게 공유할 수 있다.

### 1차 출시 성공 기준

- 로그인부터 습관 생성, 오늘 체크, 과거 기록 수정, 알림 수신까지 전체 흐름이 동작한다.
- 같은 계정으로 접속한 둘 이상의 기기에서 새로고침 후 같은 기록이 보인다.
- 수행 요일, 월/연 경계, 윤년 및 시간대 경계 테스트가 통과한다.
- 앱과 스케줄러가 서버 재시작 후 자동으로 복구된다.
- SQLite 백업 파일로 복구하는 절차가 검증된다.

## 4. 대상 사용자와 사용 환경

### 사용자

- 서비스 운영자 본인 한 명
- 공개 회원가입이나 다른 사용자 초대는 필요하지 않음
- `.env`에 설정된 아이디와 Argon2id 비밀번호 해시로 로그인
- 사용자 테이블과 계정 관리 화면은 사용하지 않음

### 환경

- iPhone 및 Android 스마트폰
- 필요시 데스크톱 웹 브라우저
- 인터넷 연결이 있는 환경
- WSL2 Ubuntu에서 24시간 실행되는 개인 서버
- 기존 외부 도메인과 HTTPS 리버스 프록시 사용

## 5. 제품 원칙

- 모바일 우선: 작은 화면과 터치 입력을 기준으로 설계한다.
- 빠른 기록: 오늘의 습관 체크는 눈에 잘 띄고 조작 단계가 짧아야 한다.
- 명확한 상태: 사용자에게 필요한 완료·미완료·추가 달성·보관 상태를 충분한 대비와 접근성 이름으로 구분하고 내부 분류 문구는 노출하지 않는다.
- 기록 보존: 습관을 더 이상 사용하지 않아도 과거 달성 데이터는 유지한다.
- 시간대 일관성: 날짜와 알림은 사용자의 IANA 시간대를 기준으로 처리한다.
- 점진적 권한 요청: 알림의 효용을 먼저 설명하고 사용자 동작 이후 권한을 요청한다.
- 접근성: 이모지나 색상만으로 의미를 전달하지 않는다.
- 유연성: 일정 변경과 비예정일의 추가 달성을 자연스럽게 인정하고 불필요한 실패감을 만들지 않는다.
- 일관된 모바일 UI: Threads 앱을 참고한 흑백 중심의 간결한 화면과 iOS Liquid Glass 느낌의 플로팅 하단 내비게이션을 사용한다.
- 예측 가능한 터치: 습관 카드의 체크박스는 달성 여부만 변경하고, 카드의 나머지 영역은 습관 상세 화면으로 이동한다.
- 낮은 시각적 소음: 페이지 제목 위의 보조 문구, 카드 안의 내부 상태 분류, 미래 날짜의 반복적인 `예정` 문구처럼 행동에 필요하지 않은 텍스트는 표시하지 않는다.

### 용어

- `예정일`: 해당 날짜에 적용되는 수행 요일 설정에 포함된 날
- `비예정일`: 해당 날짜에 적용되는 수행 요일 설정에 포함되지 않은 날
- `예정일 달성`: 예정일에 습관을 완료한 상태
- `예정일 미달성`: 완료하지 않은 채 지나간 예정일
- `추가 달성`: 비예정일에 자발적으로 습관을 완료한 상태
- `비예정일 미기록`: 비예정일에 달성 기록이 없는 상태. 실패로 간주하지 않음

## 6. 핵심 사용자 흐름

### 6.1 첫 사용과 로그인

1. 사용자가 서비스 URL에 접속한다.
2. 미리 생성된 아이디와 비밀번호로 로그인한다.
3. 인증에 성공하면 오늘 화면으로 이동한다.
4. 로그인 세션은 안전한 쿠키로 유지된다.

공개 회원가입과 계정 관리 화면은 제공하지 않는다. 비밀번호 변경 또는 분실 시 서버에서 로컬 명령으로 새 Argon2id 해시를 생성하고 `.env`의 값을 직접 교체한 뒤 컨테이너를 재시작한다.

### 6.2 습관 생성

1. 사용자가 새 습관 추가를 선택한다.
2. 이름을 지정하고, 원하면 이모지를 선택한다. 이모지를 지정하지 않으면 기본 아이콘을 사용한다.
3. 하나 이상의 수행 요일을 선택한다.
4. 필요하면 알림을 활성화하고 시간을 설정한다. 알림 요일은 수행 요일을 그대로 따른다.
5. 저장하면 해당 습관이 활성 상태로 등록된다.

### 6.3 오늘의 습관 체크

1. 사용자가 오늘 화면을 연다.
2. 오늘이 예정일인 활성 습관 목록을 확인한다.
3. 습관을 체크하면 즉시 완료 상태로 바뀐다.
4. 다시 체크를 해제하면 해당 날짜의 달성 기록이 제거된다.
5. 변경된 현재 streak가 화면에 반영된다.
6. 습관 카드는 미달성 항목을 먼저 표시하고, 각 상태 그룹 안에서는 지정된 알림 시간이 빠른 순서로 표시한다. 시간이 없는 습관은 해당 그룹의 마지막에 표시한다.
7. 체크 후에는 목록 전체가 즉시 재정렬된다.

각 습관에는 이름과 함께 현재 streak, 수행 요일 요약, 저장된 알림 시간을 표시한다. 시간은 `1:00 PM` 형태의 12시간제로 표시하고 활성 알림에는 시계 아이콘을 함께 표시한다. 알림이 꺼져 있어도 저장된 시간이 있으면 시간은 표시하며, 시간 데이터가 없으면 시간 영역을 생략한다.

### 6.4 달력에서 과거 기록 수정

1. 사용자가 달력 화면을 연다.
2. 월을 이동하고 오늘 또는 과거 날짜를 선택한다.
3. 기본 목록에서 해당 날짜에 예정된 습관과 이미 기록된 추가 달성을 확인한다.
4. 달성 여부를 체크하거나 취소한다.
5. 비예정 습관을 기록하려면 `다른 습관 기록`을 펼치고 원하는 습관의 체크박스를 누른다.
6. 기록된 비예정 습관은 추가 달성으로 기본 목록에 이동하며, 취소하면 다시 `다른 습관 기록` 영역으로 돌아간다.
7. 미래 날짜를 선택해 달성 기록을 만들 수는 없고 `다른 습관 기록`도 제공하지 않는다.

비예정일에도 달성 기록을 남길 수 있으며, 자발적인 추가 달성으로 간주해 streak에 포함한다. 비예정일에 기록을 남기지 않은 것은 실패가 아니므로 streak를 끊지 않는다.

달력의 습관 목록도 오늘 화면과 같은 메타데이터와 정렬 규칙을 사용한다. 예정되지 않았고 달성 기록도 없는 습관은 기본 목록에 노출하지 않는다.

### 6.5 수행 요일 변경

1. 사용자가 습관 수정 화면에서 수행 요일을 변경한다.
2. 새 수행 요일은 사용자의 현지 날짜 기준 변경 당일부터 적용된다.
3. 변경 전 날짜는 당시 유효했던 수행 요일을 계속 사용한다.
4. 일정 변경 자체는 현재 streak를 초기화하지 않는다.
5. 변경 당일의 예정 여부와 오늘 목록은 새 일정에 맞춰 즉시 갱신된다.

### 6.6 알림 설정과 수신

1. 사용자가 습관의 알림을 활성화한다.
2. 현지 시간을 지정한다. 반복 요일은 습관 수행 요일을 자동으로 따른다.
3. 앱이 지원 조건을 확인하고 알림 권한을 요청한다.
4. 허용된 기기의 Web Push 구독을 서버에 저장한다.
5. 지정된 시점에 해당 사용자의 등록된 모든 기기로 알림을 발송한다.
6. 알림을 누르면 앱의 오늘 화면 또는 해당 습관으로 이동한다.

### 6.7 연속 달성 공유

1. 사용자가 오늘 또는 습관들 화면에서 습관 상세로 이동한 뒤 `공유`를 선택한다.
2. 앱이 현재 streak와 습관 정보를 사용해 세로형 이미지를 생성한다.
3. 사용자는 이미지 미리보기에서 습관 이름, 이모지 또는 기본 표시, `연속 N회 달성` 문구, 습관 시작일과 선택된 배경을 확인한다.
4. 공유를 누르면 운영체제의 공유 시트가 열린다.
5. 사용자가 Instagram 등 원하는 앱을 직접 선택한다.
6. 파일 공유를 지원하지 않는 환경에서는 PNG를 다운로드해 직접 게시할 수 있다.

### 6.8 습관 탐색 및 관리

1. 화면 하단의 아이콘 전용 플로팅 내비게이션으로 `홈`, `습관`, `달력`, `설정`을 이동한다. 각 아이콘에는 접근성 이름을 제공한다.
2. `습관들` 화면에는 진행 중인 습관을 먼저 표시하고, 보관된 습관은 기본적으로 접힌 `지난 습관들` 영역에 표시한다.
3. 습관 카드에는 현재 streak, 수행 요일 요약, 저장된 알림 시간을 표시한다.
4. 수행 요일은 월~금이면 `주중`, 토~일이면 `주말`, 모든 요일이면 `매일`, 그 외에는 `월·수·금` 형태로 표시한다.
5. 습관 카드를 누르면 상세 화면으로 이동한다. 상세에는 첫 시작일, 요일, 저장된 시간, 알림 켜짐 여부를 표시하며 공유, 수정 및 삭제를 수행한다. 삭제는 기록을 보존하는 보관 동작이다. 목록 카드에는 별도 수정·공유 버튼을 두지 않는다.
6. 홈에서 연 상세·수정 화면의 뒤로가기, 취소 및 저장은 홈으로 돌아가고, 습관들 화면에서 연 경우에는 습관들 화면으로 돌아간다.

## 7. 기능 요구사항

### FR-1 인증

- 아이디와 비밀번호로 로그인할 수 있어야 한다.
- 서버는 단일 아이디와 Argon2id 비밀번호 해시를 `.env`에서 읽어야 한다.
- 사용자 테이블을 생성하거나 자격 증명을 SQLite에 저장해서는 안 된다.
- 공개 회원가입 화면과 API를 제공하지 않아야 한다.
- 비밀번호는 복원 가능한 형태로 저장하지 않아야 한다.
- 비밀번호 변경 UI와 이메일 재설정 흐름을 제공하지 않아야 한다.
- 로컬 해시 생성 명령은 비밀번호를 대화형으로 입력받고 Argon2id 해시만 출력해야 한다.
- 비밀번호를 셸 명령 인자, 명령 기록 또는 애플리케이션 로그에 남겨서는 안 된다.
- `.env`의 비밀번호 해시가 변경되고 앱이 재시작되면 기존 로그인 세션은 모두 무효화되어야 한다.
- 인증되지 않은 사용자는 습관 및 기록 데이터에 접근할 수 없어야 한다.
- 로그인 실패 메시지는 아이디 존재 여부를 노출하지 않아야 한다.
- 사용자는 명시적으로 로그아웃할 수 있어야 한다.

### FR-2 습관 관리

- 습관을 생성, 조회, 수정, 보관할 수 있어야 한다.
- 이름과 하나 이상의 수행 요일은 필수다. 이모지는 선택 사항이며 한 글자만 입력할 수 있고 비어 있으면 기본 아이콘을 표시한다.
- 새 습관에는 내장 배경 프리셋 하나를 기본 선택하고 사용자가 다른 프리셋으로 변경할 수 있어야 한다.
- 수행 요일 변경은 변경 당일부터 적용되어야 한다.
- 과거 날짜에는 변경 당시의 이전 수행 요일이 유지되어야 한다.
- 같은 날짜에 여러 번 수정하면 그 날짜에는 마지막 수행 요일 설정만 적용되어야 한다.
- 습관은 생성 시 활성 상태여야 한다.
- 보관된 습관은 오늘 목록에서 제외하되 과거 기록과 streak 계산에 필요한 데이터는 보존한다.
- 1차 출시에서는 영구 삭제보다 보관을 사용한다.
- 습관 목록에서 진행 중인 습관과 보관된 습관을 분리하고 보관된 습관은 접힌 `지난 습관들` 영역에 표시해야 한다.
- 목록 카드 선택은 습관 상세로 이동해야 하며 수정, 공유 및 기록을 보존하는 삭제는 상세 화면에서 제공해야 한다.
- 상세 및 수정 화면은 홈과 습관들 중 실제 진입 화면으로 돌아갈 수 있어야 한다.

### FR-3 달성 기록

- 습관과 사용자 현지 날짜의 조합당 달성 기록은 최대 하나여야 한다.
- 오늘 또는 과거 날짜에 달성 기록을 생성하거나 제거할 수 있어야 한다.
- 미래 날짜에는 달성 기록을 만들 수 없어야 한다.
- 중복 요청이 발생해도 달성 기록이 중복 생성되지 않아야 한다.
- 서버 저장에 실패하면 화면을 원래 상태로 복구하고 재시도 방법을 알려야 한다.
- 오늘·달력·다른 습관 기록의 체크 컨트롤은 동일한 시각 크기와 충분한 터치 영역을 사용해야 한다.
- 오늘과 달력의 습관 목록은 미달성 항목을 먼저, 각 상태 그룹 안에서는 저장된 알림 시간 오름차순으로 정렬하고 시간이 없는 항목은 마지막에 표시해야 한다.
- 달성 상태가 바뀌면 현재 화면의 목록 순서를 즉시 다시 계산해야 한다.

### FR-4 달력

- 월 단위 달력을 제공해야 한다.
- 이전 달과 다음 달로 이동할 수 있어야 한다.
- 오늘, 선택한 날짜, 미래 날짜를 구분해야 한다.
- 미래 예정일은 셀 안에 별도 마커나 반복적인 `예정` 텍스트를 표시하지 않고 접근성 이름에만 예정 정보를 유지해야 한다.
- 오늘 날짜 셀에는 `1/1` 같은 완료 분수를 중복 표시하지 않아야 한다.
- 완료·부분 달성·미달성 상태는 셀 배경이 아닌 날짜 아래 색상 점으로 표시하고 범례는 달력 테두리 밖에서 가운데 정렬해야 한다.
- 오늘 날짜는 달성 상태와 무관한 옅은 테두리로 표시해야 한다.
- 오늘의 미완료 상태도 회색 점으로 표시하고 선택한 날짜에는 별도 배경색을 적용하지 않아야 한다.
- 선택한 날짜의 습관별 달성 상태를 조회하고 변경할 수 있어야 한다.
- 선택한 날짜의 기본 목록에는 예정된 습관과 이미 기록된 추가 달성만 표시해야 한다.
- 오늘과 과거 날짜에는 비예정 습관을 선택해 추가 달성으로 기록할 수 있는 `다른 습관 기록` 영역을 제공해야 한다.
- 추가 달성을 취소한 습관은 기본 목록에서 제거하고 `다른 습관 기록` 영역으로 되돌려야 한다.
- 미래 날짜에는 `다른 습관 기록` 영역과 기록 버튼을 제공하지 않아야 한다.
- 이모지와 색상만으로 달성 상태를 전달해서는 안 된다.

### FR-5 streak

- streak는 날짜별로 당시 유효했던 수행 요일과 실제 달성 기록을 함께 사용해 계산해야 한다.
- 예정일 달성은 streak에 1회 포함해야 한다.
- 추가 달성도 streak에 1회 포함해야 한다.
- 비예정일 미기록은 streak를 늘리거나 끊지 않아야 한다.
- 오늘이 예정일이며 아직 완료하지 않았더라도 당일이 끝나기 전에는 직전까지의 streak를 보여야 한다.
- 오늘을 완료했으면 예정일 여부와 관계없이 streak에 포함해야 한다.
- 완료하지 않은 과거 예정일이 있으면 그 날짜에서 streak가 끊겨야 한다.
- 수행 요일 변경은 과거 날짜의 예정 여부나 과거 streak를 바꾸지 않아야 한다.
- 현재 streak는 일정 변경 경계를 넘어 이어질 수 있어야 한다.

예: 월·수·금 습관을 월요일과 화요일(비예정일), 수요일에 모두 완료했다면 streak는 3이다. 화요일에 완료하지 않았다면 월요일과 수요일의 연속 수행 streak는 2이며 끊기지 않는다.

예: 월·수·금 습관을 목요일에 화·목·토로 변경하면 수요일까지는 이전 일정을, 목요일부터는 새 일정을 적용한다. 변경 전에 완료한 월요일과 수요일의 streak는 유지되고, 목요일을 완료하면 streak는 3이 된다.

### FR-6 알림

- 습관 하나당 알림을 최대 하나 설정할 수 있어야 한다.
- 알림은 습관 수행 요일에서 동기화된 하나 이상의 반복 요일, 현지 시간, IANA 시간대를 포함해야 한다.
- 알림 요일은 항상 습관 수행 요일을 따라야 하며 사용자가 별도로 변경할 수 없어야 한다.
- 수행 요일을 변경하면 알림 요일도 변경 당일부터 새 수행 요일로 동기화되어야 한다.
- 알림을 활성화하거나 비활성화할 수 있어야 한다.
- 화면의 알림 시간은 12시간제 `h:mm AM/PM` 형식으로 표시해야 한다.
- 활성 알림에는 접근 가능한 시계 아이콘을 표시하고, 비활성 상태라도 저장된 시간이 있으면 시간은 표시해야 한다.
- 저장된 시간 데이터가 없는 습관에는 `알림 없음` 같은 대체 문구를 표시하지 않아야 한다.
- 같은 사용자의 여러 기기/브라우저 푸시 구독을 지원해야 한다.
- 유효하지 않거나 만료된 구독을 감지하고 정리해야 한다.
- 동일한 알림 시점에 중복 발송하지 않아야 한다.
- 운영체제나 브라우저로 인해 정확한 시각의 전달이 보장되지 않을 수 있음을 안내해야 한다.

### FR-7 PWA

- 화면 모드는 시스템 설정을 기본값으로 하며 Light와 Dark를 선택할 수 있어야 한다.
- 화면 모드 선택은 브라우저 로컬 저장소에만 저장해 기기마다 다르게 설정할 수 있어야 한다.
- 다크 모드에서도 체크박스, 버튼, 달력 상태 및 내비게이션 아이콘에 충분한 대비를 제공해야 한다.
- 스크롤바 여백은 좌우 대칭으로 유지하고 트랙은 투명하게 하되 스크롤 가능한 화면에는 막대를 표시해야 한다.

- 웹 앱 매니페스트, 앱 아이콘 및 서비스 워커를 제공해야 한다.
- 지원되는 브라우저에서 홈 화면에 설치할 수 있어야 한다.
- 서비스 워커가 Web Push를 수신하고 알림을 표시해야 한다.
- iPhone에서는 홈 화면에 설치된 웹앱과 사용자 알림 권한이 필요하다는 안내를 제공해야 한다.
- 오프라인 상태에서 체크하거나 수정하는 기능은 제공하지 않는다.
- 네트워크가 없을 때 오프라인 상태와 재연결 방법을 표시해야 한다.

### FR-8 연속 달성 공유

- 습관마다 앱에 내장된 배경화면 10종 중 하나를 선택할 수 있어야 한다.
- 배경화면 10종은 외부 서비스 없이 앱과 함께 제공되는 로컬 자산이어야 한다.
- 선택한 배경 프리셋은 습관에 저장되고 이후 수정할 수 있어야 한다.
- 공유 이미지는 PNG, 1080×1920 픽셀, 9:16 비율이어야 한다.
- 공유 이미지에는 습관 이름, 습관 이모지 또는 미지정 시 기본 표시, `연속 N회 달성` 문구, 습관 시작일과 선택된 배경화면이 포함되어야 한다.
- 공유 이미지에는 계정 아이디나 비공개 서비스 URL을 넣지 않아야 한다.
- `N`은 이미지 생성 시점에 현재 streak 규칙으로 계산한 값이어야 한다.
- 생성된 이미지를 공유 전에 미리 볼 수 있어야 한다.
- 공유 동작은 명시적인 버튼 입력으로만 시작되어야 한다.
- 브라우저가 이미지 파일 공유를 지원하면 Web Share API로 시스템 공유 시트를 열어야 한다.
- 앱은 특정 공유 대상이나 Instagram 설치 여부를 보장한다고 표현해서는 안 된다.
- Web Share API 또는 이미지 파일 공유가 지원되지 않거나 실패하면 PNG 다운로드를 제공해야 한다.
- 습관 이름이 길거나 이모지 렌더링 폭이 달라도 내용이 잘리거나 겹치지 않아야 한다.
- 모든 배경 프리셋에서 텍스트가 읽히도록 오버레이와 대비를 보장해야 한다.

## 8. 화면 요구사항

### 공통 내비게이션 및 헤더

- 모바일 하단에는 `홈`, `습관`, `달력`, `설정`의 네 아이콘만 표시하는 플로팅 내비게이션을 고정한다.
- 홈은 집, 습관은 체크리스트, 달력은 날짜 점이 없는 달력 아이콘을 사용해 서로 명확히 구분한다.
- 내비게이션은 좌우 끝이 반원에 가까운 큰 radius와 Liquid Glass 질감을 사용하고 iOS 하단 홈 인디케이터를 침범하지 않도록 안전 영역 위에 배치한다.
- 시각적 텍스트 레이블은 숨기되 각 메뉴에 접근성 이름과 현재 페이지 상태를 제공한다.
- 페이지 제목 위에 별도의 작은 보조 제목을 표시하지 않는다.
- 홈 상단에는 달력·습관 관리 바로가기를 중복 배치하지 않고 하단 내비게이션을 사용한다.

### 로그인

- 아이디 입력
- 비밀번호 입력 및 표시 전환
- 로그인 버튼
- 로그인 실패 및 네트워크 오류 메시지

### 오늘

- 현재 현지 날짜
- 오늘 수행할 활성 습관 목록
- 습관별 이모지 또는 기본 아이콘, 이름, 완료 상태, 현재 streak, 수행 요일 요약, 알림 시간
- 미달성 우선, 알림 시간 오름차순 정렬과 체크 직후 재정렬
- 완료/완료 취소 동작
- 체크박스 외 카드 영역을 통한 습관 상세 진입
- 습관 추가 진입점
- 오늘 수행할 습관이 없는 빈 상태

### 습관들 및 상세

- 진행 중인 습관 목록
- 현재 streak, 수행 요일 요약, 12시간제 알림 시간
- 우측 끝의 상세 이동 화살표
- 접힌 `지난 습관들` 영역
- 습관 상세의 첫 시작일·요일·시간·알림 상태와 공유, 수정 및 기록을 보존하는 삭제 동작
- 진입 화면에 따른 뒤로가기 경로

### 습관 생성 및 수정

- 이름 입력
- 이모지 한 글자 선택(선택 사항, 미지정 시 기본 아이콘)
- `습관 이름·이모지`, `요일·시간·알림 받기`, `공유 배경`의 세 영역
- 요일과 시간은 구분선 없이 같은 층위에 표시하고 `알림 받기`는 별도 컨트롤 영역으로 구분
- 내장 배경화면 10종 선택
- 알림 받기 여부
- 알림 시간 선택과 수행 요일을 따르는 반복 요일 안내
- 영역 밖에 배치된 저장 및 취소 동작
- 입력 검증 메시지

### 달력

- 월 이동
- 날짜별 완료 상태 요약
- 선택한 날짜에 예정된 습관과 이미 기록된 추가 달성 목록
- 현재 streak, 수행 요일 요약, 12시간제 알림 시간
- 접을 수 있는 `다른 습관 기록` 목록과 추가 달성 체크박스
- 오늘과 과거 날짜의 완료/취소 동작
- 미래 날짜의 읽기 전용 상태
- 오늘 날짜의 간결한 표시와 완료·부분 달성·미달성의 초록·노랑·회색 상태 표현
- 선택 날짜는 배경색 없이 테두리로만 구분
- 선택 날짜 제목은 날짜 자체로 표시하고 `선택한 날짜` 같은 별도 영역 제목은 표시하지 않음
- 습관 카드에는 `예정일 달성`, `비예정일 미기록` 같은 내부 분류 문구를 표시하지 않음

### 설정

- 기기 IANA 시간대 자동 감지·적용과 현재 시간대 확인
- 기기별 알림 지원/권한 상태
- 알림 권한 요청 및 구독 해제
- 시스템 설정·Light·Dark 중 기기별 화면 모드 선택
- 알림 권한 아래에서 우측 정렬된 아이콘 전용 화면 모드 선택 컨트롤
- 로그아웃

### 공유 이미지 미리보기

- 9:16 이미지 미리보기
- 습관 이름, 이모지, `연속 N회 달성` 및 습관 시작일 표시
- 현재 선택된 습관 배경화면
- 텍스트 없이 접근성 이름을 제공하는 동일 색상의 공유·다운로드 아이콘 버튼
- 이미지 생성, 공유 취소 및 오류 상태

## 9. 데이터 요구사항

### AppSettings

- 기본 IANA 시간대
- 생성 및 수정 시각

인증 정보는 이 테이블에 저장하지 않는다. 아이디, Argon2id 비밀번호 해시 및 세션 비밀 키는 운영 `.env`에서만 읽는다.

### Habit

- 이름
- 이모지(선택, 빈 값 허용)
- 공유 배경 프리셋 식별자
- 활성/보관 상태
- 생성 및 수정 시각

### HabitSchedule

- 습관 식별자
- 수행 요일 집합
- 적용 시작 현지 날짜
- 적용 종료 현지 날짜(선택)
- 한 날짜에 습관별 유효 일정 최대 하나

### HabitCompletion

- 습관 식별자
- 사용자 현지 날짜
- 생성 및 수정 시각
- `(habit_id, local_date)` 유일성 제약

### Reminder

- 습관 식별자
- 습관 수행 요일에서 동기화된 반복 요일 집합
- 현지 시간
- IANA 시간대
- 활성 상태
- `habit_id` 유일성 제약

### PushSubscription

- 브라우저가 발급한 endpoint 및 암호화 키
- 마지막 성공 및 실패 시각
- 활성 상태

### NotificationDelivery

- 알림 식별자
- 예정 발송 시각
- 실제 처리 시각
- 결과 상태
- `(reminder_id, scheduled_for)` 유일성 제약

## 10. 날짜와 시간 규칙

- 달성 기록은 UTC timestamp가 아니라 사용자의 현지 달력 날짜를 기준으로 식별한다.
- 수행 요일 변경은 사용자의 현지 변경 날짜부터 적용하며 과거 날짜에는 소급하지 않는다.
- 서버 시각과 감사용 timestamp는 UTC로 저장한다.
- 알림과 streak 계산에는 IANA 시간대를 사용한다.
- 시스템 기본 시간대에 암묵적으로 의존하지 않는다.
- 사용자가 시간대를 변경하면 기존 알림은 새 시간대에서도 설정된 현지 시각을 유지한다.
- 월 경계, 연 경계, 윤년 및 DST 전환을 테스트한다.

## 11. 비기능 요구사항

### 보안

- `HABIT_TRACKER_USERNAME`, `HABIT_TRACKER_PASSWORD_HASH`, `SESSION_SECRET`을 운영 환경 변수로 사용한다.
- 비밀번호는 Argon2id 해시로만 `.env`에 저장하고 평문 비밀번호는 저장하지 않는다.
- 운영 `.env`는 Git과 Docker 이미지에서 제외하고 파일 권한 `600`으로 관리한다.
- `.env.example`에는 실제 값 없이 변수 이름과 설명만 포함한다.
- 환경 변수 값은 로그, 오류 응답 또는 진단 화면에 출력하지 않는다.
- 인증 세션에는 `Secure`, `HttpOnly`, 적절한 `SameSite` 속성을 적용한다.
- 세션 검증은 현재 비밀번호 해시의 변경을 감지해 기존 세션을 무효화해야 한다.
- 상태 변경 요청에는 CSRF 방어를 적용한다.
- 로그인 요청에 속도 제한을 적용한다.
- 사용자 입력을 서버에서 검증하고 출력 시 이스케이프한다.
- VAPID 개인 키와 세션 비밀 키를 환경 변수 또는 Docker secret으로 관리한다.
- API 컨테이너를 인터넷에 직접 노출하지 않는다.

### 성능과 신뢰성

- 오늘 화면은 개인 서버의 정상 네트워크 환경에서 체감상 즉시 사용할 수 있어야 한다.
- SQLite는 WAL 모드와 `busy_timeout`을 사용한다.
- 쓰기 트랜잭션은 짧게 유지하고 잠금 충돌을 제한적으로 재시도한다.
- API와 알림 스케줄러는 각각 단일 프로세스로 운영한다.
- 알림 스케줄러가 재시작돼도 중복 없이 미처리 작업을 다시 판단할 수 있어야 한다.

### 접근성

- 모든 입력과 버튼에 접근 가능한 이름을 제공한다.
- 핵심 기능은 키보드로 조작할 수 있어야 한다.
- 터치 대상은 모바일에서 누르기 쉬운 크기와 간격을 가져야 한다.
- 상태를 색상과 이모지에만 의존해 표현하지 않는다.
- 포커스 표시와 충분한 명도 대비를 제공한다.

### 운영과 복구

- Docker Compose의 health check와 재시작 정책을 설정한다.
- Windows/WSL2 재시작 뒤 서비스 자동 복구를 점검한다.
- SQLite DB는 WSL2 Linux 파일시스템 또는 Docker named volume에 저장한다.
- SQLite Online Backup API 또는 `VACUUM INTO` 방식으로 매일 백업한다.
- 백업 보존 기간을 설정하고 복구 절차를 실제로 검증한다.
- 애플리케이션 및 알림 발송 오류를 로그로 확인할 수 있어야 한다.

## 12. 기술 구조

- 서버 및 화면 렌더링: Python, FastAPI, Jinja2
- 화면 상호작용: HTMX와 최소한의 JavaScript
- 애플리케이션 서버: Uvicorn 단일 프로세스
- 데이터베이스: SQLite
- ORM 및 마이그레이션: SQLAlchemy 2.x, Alembic
- 예약 및 푸시: 별도 단일 Python 스케줄러 프로세스, Web Push
- 공유 이미지: 고정된 1080×1920 렌더링 템플릿과 내장 배경 프리셋 10종
- 모바일 공유: Web Share API 파일 공유, 미지원 시 PNG 다운로드
- 배포: WSL2 Ubuntu, Docker Compose, 기존 HTTPS 리버스 프록시
- PWA: 웹 앱 매니페스트, 서비스 워커, 앱 아이콘

React, 별도 SPA 프레임워크, Supabase 및 PostgreSQL은 1차 출시에 사용하지 않는다.

## 13. 1차 출시 수용 기준

- 올바른 아이디와 비밀번호로 로그인할 수 있고 잘못된 자격 증명은 거부된다.
- 사용자 테이블 없이 `.env`의 자격 증명만으로 인증된다.
- `.env`의 비밀번호 해시를 교체하고 재시작하면 이전 세션으로 접근할 수 없다.
- 이름과 수행 요일을 입력해 습관을 생성할 수 있고, 이모지는 선택적으로 한 글자만 지정할 수 있다.
- 오늘 수행할 습관을 체크하거나 취소할 수 있다.
- 새로고침하거나 다른 기기로 접속해도 저장된 상태가 유지된다.
- 달력에서 과거 날짜의 달성 여부를 추가하거나 취소할 수 있다.
- 달력의 기본 목록에는 예정된 습관과 이미 기록된 추가 달성만 보이고, 비예정 습관은 `다른 습관 기록`에서 추가 달성으로 기록할 수 있다.
- 오늘과 달력의 목록은 미달성 우선 및 알림 시간 오름차순으로 정렬되며 체크 후 즉시 재정렬된다.
- 미래 날짜에는 달성 기록을 추가할 수 없다.
- 예정일 미달성과 추가 달성을 함께 반영해 현재 streak가 정확히 계산된다.
- 수행 요일을 변경해도 변경 전 날짜의 예정 여부와 과거 streak가 바뀌지 않는다.
- 습관별로 알림 하나를 설정하고 등록된 iPhone과 Android에서 받을 수 있다.
- 알림 권한 거부와 미지원 상태가 사용자에게 설명된다.
- 습관별 내장 배경을 선택하고 이름, 이모지 또는 기본 표시, 현재 streak가 포함된 1080×1920 PNG를 생성할 수 있다.
- 지원되는 iPhone과 Android에서 시스템 공유 시트로 이미지를 전달할 수 있다.
- 파일 공유 미지원 또는 실패 환경에서 같은 PNG를 다운로드할 수 있다.
- 습관을 보관해도 과거 기록이 유지된다.
- 네트워크가 끊기면 저장된 것처럼 오인시키지 않고 오프라인 상태를 표시한다.
- 자동 테스트, 타입 검사, 린트 및 주요 모바일 화면 검증이 통과한다.

## 14. 출시 단계

### 단계 1: 기반

- FastAPI/Jinja2/HTMX 프로젝트 구조
- Docker Compose와 환경 변수
- SQLite 연결, 마이그레이션 및 백업 기반
- Argon2id 비밀번호 해시 생성 명령과 `.env.example`
- 환경 변수 기반 단일 계정 로그인/로그아웃 및 세션 무효화

### 단계 2: 핵심 습관 기능

- 습관 생성, 수정 및 보관
- 오늘 화면과 달성 체크
- 일정 이력, 예정일 및 추가 달성을 반영한 streak 계산

### 단계 3: 기록 탐색

- 월간 달력
- 과거 날짜 기록 조회 및 수정
- 경계 날짜와 시간대 테스트

### 단계 4: PWA와 알림

- 매니페스트, 아이콘 및 서비스 워커
- 기기별 푸시 구독
- Python 스케줄러와 중복 방지
- iPhone/Android 설치 및 실기기 알림 검증

### 단계 5: 공유

- 내장 배경화면 10종과 습관별 선택 UI
- 1080×1920 공유 이미지 생성 및 미리보기
- Web Share API와 PNG 다운로드 대체 흐름
- iPhone/Android에서 Instagram 전달 흐름 실기기 검증

### 단계 6: 운영 준비

- 보안 설정과 로그인 속도 제한
- health check, 재시작 및 로그
- 자동 백업과 복구 리허설
- 접근성 및 모바일 사용성 점검

## 15. 범위 밖

- 네이티브 iOS 또는 Android 앱
- 공개 회원가입과 다중 사용자 서비스
- 친구/가족 공유, 소셜 피드 및 경쟁 기능
- 완전한 오프라인 체크, 수정 및 충돌 동기화
- 건강 데이터와 웨어러블 연동
- 유료 구독과 결제
- 의료 조언이나 성과 예측
- Instagram 계정 연동, 특정 앱으로의 강제 공유, 자동 게시 및 게시 결과 추적
- 사용자 배경 이미지 업로드와 공유 이미지 자유 편집
- 고가용성, 다중 서버 및 수평 확장

## 16. 향후 재검토 조건

다음 중 하나가 발생하면 SQLite와 단일 서버 구조를 재검토한다.

- 여러 사용자가 동시에 활발히 기록하는 서비스로 변경
- 여러 API 또는 스케줄러 인스턴스가 동시에 DB에 써야 함
- 데이터베이스를 네트워크 파일시스템에 두어야 함
- 고가용성, 무중단 장애 조치 또는 수평 확장이 필요함

이 경우 PostgreSQL과 별도 작업 큐 도입을 우선 검토한다.
