# Habit Tracker 프로젝트 지침

## 프로젝트 목표

여러 스마트폰과 브라우저에서 같은 데이터를 사용할 수 있는 개인용 습관 트래커를 만든다. 첫 번째 제품은 iPhone과 Android를 모두 지원하는 모바일 우선 설치형 웹앱(PWA)으로 구현한다.

핵심 경험은 다음과 같다.

- 사용자가 지키고 싶은 습관을 만들고 수정하고 보관할 수 있다.
- 습관을 만들 때 이름을 입력하고 이모지를 선택적으로 지정할 수 있다. 이모지가 없으면 기본 아이콘을 사용한다.
- 오늘 또는 달력에서 선택한 과거 날짜의 달성 여부를 체크하거나 취소할 수 있다.
- 각 습관의 현재 연속 달성 일수(streak)를 볼 수 있다.
- 습관마다 수행 요일을 지정할 수 있다.
- 습관마다 수행 요일을 따르는 알림 시간 한 세트를 설정할 수 있다.
- 습관별 배경화면과 현재 streak로 세로형 공유 이미지를 만들어 Instagram 등에 공유할 수 있다.
- 로그인한 사용자의 습관과 달성 기록이 여러 기기에서 동기화된다.

## 제품 원칙

- 모바일 화면과 터치 조작을 먼저 설계하고 데스크톱에서도 자연스럽게 확장한다.
- 오늘의 습관을 확인하고 체크하는 핵심 동작은 최소한의 탭으로 끝나야 한다.
- 날짜와 시간 계산은 사용자의 IANA 시간대(예: `Asia/Seoul`)를 기준으로 한다.
- 체크 취소, 과거 기록 수정 등 사용자의 명시적인 입력은 즉시 반영하고 동기화 실패 시 분명하게 알린다.
- 접근성을 기본 요구사항으로 취급한다. 이모지만으로 의미를 전달하지 않고 텍스트 레이블, 키보드 조작, 충분한 대비를 제공한다.
- 알림 권한은 기능의 이점을 설명한 뒤 사용자 동작을 통해 요청한다.
- 사용자의 자기 개선을 돕되 엄격한 통제보다 일정 변경과 추가 달성을 자연스럽게 인정하는 유연성을 우선한다.
- Threads 앱을 참고한 흑백 중심의 간결한 UI와 iOS Liquid Glass 느낌의 아이콘 전용 플로팅 하단 내비게이션을 유지한다.
- 습관 카드의 체크박스는 달성 상태만 변경하고 나머지 영역은 습관 상세 화면으로 이동한다.
- 하단 내비게이션은 `홈`, `습관`, `달력`, `설정` 네 메뉴로 구성하고 iOS 홈 인디케이터 안전 영역 위에 배치한다. 시각적 글자는 숨겨도 접근성 이름은 유지한다.
- 페이지 제목 위의 작은 보조 문구와 홈 상단의 중복 달력·습관 관리 버튼은 두지 않는다.

## 1차 출시 범위

### 습관 관리

- 습관 생성, 조회, 수정, 보관(archive)
- 필수 필드: 이름
- 이모지는 선택 사항이며 미지정 시 기본 습관 아이콘을 표시
- 수행할 요일을 하나 이상 지정
- 공유 이미지에 사용할 내장 배경화면 10종 중 하나를 선택
- 수행 요일 변경은 사용자의 현지 날짜 기준 변경 당일부터 적용
- 수행 요일 변경 이력을 보존하고 과거 날짜에 소급 적용하지 않음
- 생성 시 활성 상태가 기본값
- 삭제보다 보관을 우선하여 기존 달성 기록을 보존
- 진행 중인 습관은 `습관들` 목록에 표시하고 보관된 습관은 기본적으로 접힌 `지난 습관들` 영역에 표시
- 목록의 습관 카드는 상세 화면으로 이동하며 편집, 공유, 보관은 상세 화면에서 제공
- 상세 화면의 `삭제`는 과거 기록과 일정을 보존하는 보관 동작으로 처리
- 상세 화면에는 첫 일정 적용일, 요일, 저장된 시간, 알림 켜짐 여부를 표시하고 공유 배경 정보는 표시하지 않음
- 상세·편집 화면의 뒤로가기, 취소, 저장 후 이동은 홈 또는 습관들 중 실제 진입 화면을 유지

### 달성 기록

- 습관과 현지 날짜의 조합당 달성 기록은 최대 하나
- 오늘 및 과거 날짜를 체크하거나 체크 해제 가능
- 미래 날짜는 체크 불가
- 월간 달력에서 날짜별 달성 상태를 확인하고 수정 가능
- 미래 예정일은 별도 마커나 반복적인 `예정` 문구를 표시하지 않고 접근성 이름에만 예정 정보를 유지하며 오늘 날짜 셀에는 완료 분수를 표시하지 않음
- 달력의 완료·부분 달성·미달성은 날짜 아래 색상 점으로 표시하고 오늘은 상태와 무관한 옅은 테두리로 구분
- 달력은 오늘을 짧은 선으로 표시하고 완료·부분 달성·미달성을 초록·노랑·회색으로 구분
- 선택 날짜의 습관 카드에는 `예정일 달성`, `비예정일 미기록` 같은 내부 상태 분류 문구를 표시하지 않음
- 오늘과 달력 목록은 미달성 습관을 먼저 표시하고, 각 상태 그룹 안에서는 저장된 알림 시간이 빠른 순서로 정렬하며 시간이 없는 습관은 마지막에 표시
- 체크 또는 체크 취소 후 목록을 즉시 재정렬
- 달력의 기본 목록에는 해당 날짜의 예정 습관과 이미 기록된 추가 달성만 표시
- 비예정 습관은 오늘과 과거 날짜의 `다른 습관 기록` 영역에서 체크해 추가 달성으로 기록하며 미래 날짜에는 이 영역을 표시하지 않음

### 연속 달성 일수

- `예정일`은 해당 날짜에 유효한 수행 요일 설정에 포함된 날이고, `비예정일`은 포함되지 않은 날
- 현재 streak는 사용자의 현지 날짜, 날짜별로 당시 유효했던 수행 요일, 실제 달성 기록을 함께 사용해 계산
- 예정일 달성은 streak에 1회 포함
- 비예정일 달성은 `추가 달성`으로 보고 streak에 1회 포함
- 비예정일 미기록은 streak를 늘리거나 끊지 않고 건너뜀
- 완료하지 않은 과거 예정일이 있으면 streak가 끊김
- 오늘이 예정일이고 아직 완료하지 않았더라도 당일이 끝나기 전에는 streak를 끊지 않고 직전까지의 값을 표시
- 오늘 기록이 있으면 예정일 여부와 관계없이 streak에 포함
- 수행 요일 변경 전 날짜는 당시 일정을, 변경 당일과 이후 날짜는 새 일정을 적용하며 변경 자체로 streak를 초기화하지 않음
- 보관된 습관도 과거 기록과 당시 streak 계산에 필요한 데이터는 유지

### 알림

- 습관별로 수행 요일과 현지 시간으로 구성된 알림 하나를 지원
- 알림 요일은 항상 해당 날짜에 유효한 습관 수행 요일을 따름
- 알림 설정에는 별도의 요일 선택 메뉴를 제공하지 않음
- 수행 요일을 변경하면 알림 요일도 변경 당일부터 새 수행 요일을 따름
- 알림 설정에는 시간대도 저장
- 설정 화면에서 기기의 IANA 시간대를 자동 감지해 앱 설정과 기존 알림에 적용하고 저장된 현지 시간은 유지
- 기기별 푸시 구독을 관리하고 만료되거나 거부된 구독을 정리
- 서버의 예약 작업이 알림 발송 시점을 결정하며 서비스 워커가 푸시를 표시
- 알림 전달은 운영체제와 브라우저 정책의 영향을 받으므로 정확한 초 단위 전달을 보장하지 않음
- 알림을 사용할 수 없는 환경에는 지원 조건과 설정 방법을 안내
- 목록의 알림 시간은 `1:00 PM` 형태의 12시간제로 표시하고 활성 알림에만 시계 아이콘을 표시
- 알림이 비활성화되어도 저장된 시간은 표시하며 시간 데이터가 없으면 대체 문구 없이 생략

### 달성 공유

- 습관별 공유 버튼에서 Instagram Story용 1080×1920(9:16) PNG 생성
- 공유 이미지의 필수 요소는 습관 이름, 습관 이모지 또는 미지정 시 기본 표시, `연속 N회 달성` 문구, 해당 습관에 지정된 배경화면
- 배경화면은 앱에 포함된 10종의 프리셋 중 습관별로 하나를 선택하고 저장
- 공유 전에 완성된 이미지 미리보기를 제공
- 지원 환경에서는 사용자 동작으로 Web Share API의 파일 공유 시트를 열고 사용자가 Instagram 등 대상을 선택
- 웹앱에서 Instagram을 강제로 열거나 자동 게시된다고 가정하지 않음
- 파일 공유를 지원하지 않거나 공유에 실패한 환경에서는 PNG 다운로드 제공
- 공유 이미지는 생성 시점의 습관 이름, 이모지, 현재 streak 및 배경 설정을 사용

## 권장 기술 방향

다음 구성을 기본 기술 방향으로 사용한다.

- 클라이언트: FastAPI가 렌더링하는 Jinja2 템플릿, HTMX, 최소한의 JavaScript로 구성한 반응형 PWA
- 프론트엔드에 React 또는 별도 SPA 프레임워크를 사용하지 않음
- 서버 애플리케이션: Python과 FastAPI 기반의 인증된 API
- 데이터베이스: 서버의 로컬 Linux 파일시스템에 저장하는 SQLite
- 데이터 접근 및 마이그레이션: SQLAlchemy 2.x와 Alembic을 우선 사용
- 인증: `.env`의 단일 아이디와 Argon2id 비밀번호 해시를 사용하는 로그인. 사용자 테이블, 공개 회원가입, 계정 관리 UI는 제공하지 않음
- 알림: 별도의 단일 Python 스케줄러 프로세스가 발송 대상을 조회하고 Web Push 발송
- 동기화: 서버를 최종 데이터 원본으로 사용하고 중복 요청에도 안전한 API 설계
- 배포: WSL2 Ubuntu의 Docker Compose로 24시간 운영하며 기존 도메인과 HTTPS 리버스 프록시를 사용

인터넷 연결이 끊긴 상태에서 습관을 체크하거나 수정하는 기능은 지원하지 않는다. 서비스 워커는 앱 설치, 정적 자산 캐시, Web Push 수신에 필요한 범위로 제한하며 오프라인 쓰기 큐나 충돌 병합은 구현하지 않는다.

Supabase와 PostgreSQL은 현재 범위에 사용하지 않는다. 향후 사용자가 늘어나거나 여러 서버 인스턴스가 동시에 DB에 쓰는 구조가 필요해질 때 PostgreSQL 이전을 검토한다.

인증 환경 변수는 `HABIT_TRACKER_USERNAME`, `HABIT_TRACKER_PASSWORD_HASH`, `SESSION_SECRET`을 기본 이름으로 사용한다. 비밀번호 변경은 로컬 해시 생성 명령으로 새 Argon2id 해시를 만든 뒤 `.env`를 직접 수정하고 컨테이너를 재시작하는 방식으로만 수행한다. 해시 생성 명령은 비밀번호를 셸 인자, 명령 기록 또는 로그에 남기지 않아야 한다. 비밀번호 해시가 바뀌면 기존 로그인 세션을 모두 무효화한다.

`.env`, 세션 비밀 키, 푸시 발송 키는 저장소나 Docker 이미지에 포함하지 않는다. `.env.example`에는 실제 값 없이 변수 이름과 설명만 두고, 운영 `.env`는 파일 권한 `600`으로 관리한다. 애플리케이션 로그와 오류 응답에 환경 변수 값을 출력하지 않는다.

## SQLite 운영 규칙

- DB 파일은 Windows 마운트(`/mnt/c` 등)가 아닌 WSL2의 Linux 파일시스템 또는 Docker named volume에 둔다.
- WAL 모드를 사용하고 `busy_timeout`을 설정하며 쓰기 트랜잭션을 짧게 유지한다.
- SQLite는 동시 writer가 하나라는 전제로 설계한다. 예상 가능한 잠금 충돌은 제한적으로 재시도한다.
- SQLite 3.51.3 이상 또는 WAL-reset 수정이 백포트된 3.50.7/3.44.6 이상을 사용한다. 컨테이너 빌드와 시작 시 실제 버전을 검증한다.
- API 서버와 알림 스케줄러는 각각 하나의 프로세스로 시작한다. 스케줄러를 API worker 내부에서 함께 실행하지 않는다.
- 스케줄러 작업은 `(reminder_id, scheduled_for)` 등의 유일성 키를 사용하여 중복 발송에 안전하게 만든다.
- 실행 중인 DB 파일을 단순 복사하지 않는다. SQLite Online Backup API 또는 `VACUUM INTO` 기반으로 매일 백업하고 보존 기간을 정한다.
- DB 파일뿐 아니라 필요한 WAL 상태까지 고려해 복구 절차를 테스트한다.

## 최소 데이터 모델

이름은 실제 기술 스택의 관례에 맞춰 조정할 수 있지만 의미와 제약은 유지한다.

- `AppSettings`: 기본 IANA 시간대 등 비밀이 아닌 단일 사용자 설정
- `Habit`: 식별자, 이름, 선택 이모지(빈 값 허용), 공유 배경 프리셋 식별자, 활성/보관 상태, 생성/수정 시각
- `HabitSchedule`: 식별자, 습관 식별자, 수행 요일 집합, 적용 시작 현지 날짜, 적용 종료 현지 날짜(선택)
  - 한 날짜에는 습관별로 유효한 일정이 최대 하나
  - 같은 날 수행 요일을 여러 번 변경하면 해당 날짜에 시작한 최신 일정을 교체하고 불필요한 이력 행을 만들지 않음
- `HabitCompletion`: 식별자, 습관 식별자, 사용자 현지 날짜, 생성/수정 시각
  - `(habit_id, local_date)`에 유일성 제약
- `Reminder`: 식별자, 습관 식별자, 수행 요일에서 동기화된 요일 집합, 현지 시간, IANA 시간대, 활성 상태
  - `habit_id`에 유일성 제약을 두어 습관당 알림을 최대 하나로 제한
- `PushSubscription`: 식별자, 기기/브라우저 구독 정보, 마지막 성공/실패 시각

사용자 테이블과 사용자 외래 키는 만들지 않는다. 모든 애플리케이션 데이터는 단일 계정 소유로 간주하며, 인증된 세션 없이는 어떤 데이터에도 접근할 수 없게 한다.

## 날짜 및 시간 규칙

- 달성 기록의 기준은 UTC 시각이 아니라 사용자의 현지 달력 날짜다.
- 수행 요일 변경은 사용자의 현지 변경 날짜부터 적용하고 과거 날짜에는 소급하지 않는다.
- 서버 시각과 저장 시각은 UTC로 보관한다.
- 알림 예약에는 IANA 시간대를 사용하고 DST 변화를 고려한다.
- 사용자가 시간대를 변경했을 때 기존 알림을 새 시간대에 맞출지 기존 지역 시간을 유지할지는 UI에서 명확히 안내한다.
- 날짜 계산을 문자열 또는 밀리초 산술로 직접 구현하지 말고 검증된 날짜/시간 API를 사용한다.

## 배포 토폴로지

- 리버스 프록시: 기존 HTTPS 종단과 도메인을 사용하며 API 컨테이너를 인터넷에 직접 노출하지 않음
- 프론트엔드: FastAPI 컨테이너가 Jinja2 템플릿과 정적 자산을 제공
- API: 단일 FastAPI/Uvicorn 프로세스
- Scheduler: 알림 조회와 Web Push 발송만 담당하는 단일 Python 프로세스
- Data: 영속 Docker volume의 SQLite DB와 별도 백업 위치

컨테이너에는 health check와 재시작 정책을 설정한다. WSL2 또는 Windows 재시작 후 Docker Compose 스택이 자동 복구되는지도 운영 점검 항목에 포함한다.

## 구현 및 품질 기준

- 현재 저장소의 기존 패턴과 도구가 생기면 이를 우선한다. 새 의존성은 필요성과 유지 비용을 확인한 뒤 추가한다.
- UI, 도메인 규칙, 데이터 접근, 알림 발송을 분리한다.
- API 입력은 서버에서 검증하고 모든 애플리케이션 데이터 접근에 인증 검사를 적용한다.
- 낙관적 UI를 사용한다면 실패 시 원래 상태로 복구하고 사용자에게 재시도 경로를 제공한다.
- 네트워크가 끊겼을 때 오프라인 상태와 재연결 방법을 안내한다. 오프라인 체크나 수정 기능은 제공하지 않는다.
- 핵심 상태에는 로딩, 빈 상태, 오류, 권한 거부 상태를 모두 구현한다.
- 기능을 추가할 때 관련 테스트도 함께 추가한다.

최소 테스트 대상:

- 오늘 완료/미완료에 따른 streak 계산
- 추가 달성의 streak 포함, 비예정일 미기록 건너뛰기, 누락된 예정일의 streak 중단
- 수행 요일 변경 경계에서 과거 일정 보존과 현재 streak 연속성
- 누락된 날짜, 월 경계, 연 경계, 윤년에서의 streak 계산
- 시간대 및 DST 경계의 알림 예약
- 같은 날짜의 중복 달성 방지와 체크 해제
- 인증되지 않은 요청의 습관, 기록 및 설정 접근 차단
- 환경 변수 자격 증명 검증, 비밀번호 해시 변경 후 기존 세션 무효화
- 푸시 권한 허용, 거부, 미지원 상태
- 1080×1920 공유 이미지의 내용과 레이아웃, Web Share 지원/미지원 및 다운로드 대체 흐름
- 모바일 크기의 핵심 흐름과 달력 키보드 접근성
- 오늘·달력의 미달성 우선 및 알림 시간 정렬, 체크 후 재정렬
- 달력의 예정 습관 기본 목록과 `다른 습관 기록` 추가·취소 흐름

## 작업 완료 조건

작업은 다음 조건을 만족할 때 완료로 본다.

- 요청된 사용자 흐름이 실제 UI와 서버 동작으로 끝까지 연결됨
- 정상 상태뿐 아니라 로딩, 빈 상태, 오류 상태가 처리됨
- 날짜/시간대와 인증 경계 사례가 검증됨
- 관련 자동 테스트, 타입 검사, 린트가 통과함
- 사용자에게 보이는 동작이나 설정 방법이 바뀌면 문서도 갱신됨
- 접근성 이름과 키보드/터치 조작을 확인함

## 범위 밖(별도 요청 전까지)

- 네이티브 iOS/Android 앱
- 소셜 피드, 친구 경쟁, 공개 프로필
- 건강 데이터 및 웨어러블 연동
- 유료 구독과 결제
- 의료적 조언이나 성과 예측
- 오프라인 상태에서의 습관 체크 및 수정과 이후 동기화
- Instagram 계정 연동, 자동 게시 및 게시 결과 추적
- 사용자 배경 이미지 업로드와 자유 편집
